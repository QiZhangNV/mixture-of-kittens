diff --git a/include/ops/thread/mma/tcgen05.cuh b/include/ops/thread/mma/tcgen05.cuh
index 3da6b991..665f5bef 100644
--- a/include/ops/thread/mma/tcgen05.cuh
+++ b/include/ops/thread/mma/tcgen05.cuh
@@ -149,7 +149,7 @@ __device__ static inline constexpr uint32_t instruction_descriptor() {
     return desc;
 };
 
-template<typename D, typename AB, typename SAB, int M, int N, bool neg=false, int scale_factor_id=0, int mma_k_bytes=32>
+template<typename D, typename AB, typename SAB, int M, int N, bool neg=false, int scale_factor_id=0, int mma_k_bytes=32, bool transpose_b=false>
 __device__ static inline constexpr uint32_t instruction_descriptor() {
     // Only supported types are MXFP8 and NVFP4
     static_assert(std::is_same_v<AB, fp8e4m3> || std::is_same_v<AB, fp4e2m1_2>, "AB must be fp8e4m3 for f4e2m1");
@@ -184,7 +184,7 @@ __device__ static inline constexpr uint32_t instruction_descriptor() {
     }
     desc |= 0b0 << 14; // Don't negate B matrix (in all cases)
     desc |= 0b0 << 15; // Don't transpose A (in all cases)
-    desc |= 0b0 << 16; // Don't transpose B (in all cases)
+    desc |= (transpose_b ? 0b1 : 0b0) << 16;
     desc |= (N >> 3) << 17; // B matrix has dimension N, encoded
     desc |= scale_type   << 23; // Scale type (0 is ue4m3, 1 is ue8m0)
     desc |= 0b000 << 24; // SBZ
@@ -571,7 +571,9 @@ __device__ static inline void mma(D &d, const A &a, const B &b, const SA &sa, co
 
     constexpr int block_size = std::is_same_v<typename SA::T, fp8e4m3> ? 16 : 32;
     constexpr int trans_b = 1 - n_trans_b;
-    static_assert(trans_a == transpose::N && trans_b == transpose::N, "Only ABt supported for microscaling formats currently");
+    static_assert(trans_a == transpose::N, "Transposed A is not supported for microscaling formats");
+    static_assert(trans_b == transpose::N || (std::is_same_v<typename A::T, fp8e4m3> && mma_k_bytes == 32),
+                  "MN-major B is only supported for MXFP8 K32");
 #if !defined(KITTENS_SM103)
     static_assert(mma_k_bytes == 32, "32B NVFP4 tcgen05 MMA is only supported on non-SM103.");
 #endif
@@ -608,10 +610,10 @@ __device__ static inline void mma(D &d, const A &a, const B &b, const SA &sa, co
 
     // Generate instruction descriptors
     constexpr uint32_t idescs[4] = {
-        detail::tcgen05::instruction_descriptor<T_D, T_AB, T_SAB, M, N, false, 0, mma_k_bytes>(),
-        detail::tcgen05::instruction_descriptor<T_D, T_AB, T_SAB, M, N, false, 1, mma_k_bytes>(),
-        detail::tcgen05::instruction_descriptor<T_D, T_AB, T_SAB, M, N, false, 2, mma_k_bytes>(),
-        detail::tcgen05::instruction_descriptor<T_D, T_AB, T_SAB, M, N, false, 3, mma_k_bytes>()
+        detail::tcgen05::instruction_descriptor<T_D, T_AB, T_SAB, M, N, false, 0, mma_k_bytes, trans_b>(),
+        detail::tcgen05::instruction_descriptor<T_D, T_AB, T_SAB, M, N, false, 1, mma_k_bytes, trans_b>(),
+        detail::tcgen05::instruction_descriptor<T_D, T_AB, T_SAB, M, N, false, 2, mma_k_bytes, trans_b>(),
+        detail::tcgen05::instruction_descriptor<T_D, T_AB, T_SAB, M, N, false, 3, mma_k_bytes, trans_b>()
     };
 
     // Offsets for moving the scales
@@ -656,7 +658,8 @@ __device__ static inline void mma(D &d, const A &a, const B &b, const SA &sa, co
             detail::tcgen05::template st_st<T_AB, T_SAB, 1, ncta, block_size>(
                 d.addr,
                 a_desc.chunk_descriptor(i),
-                b_desc.chunk_descriptor(i),
+                // MN-major FP8 shared tiles advance in 16-row atoms; one K32 MMA spans two atoms.
+                b_desc.chunk_descriptor(trans_b ? i * (red_dim / kittens::TILE_ROW_DIM<T_AB>) : i),
                 sa.addr + (i >> 2) * M_offset, // i / 4
                 sb.addr + (i >> 2) * N_offset, // i / 4
                 idescs[i % 4]
@@ -882,6 +885,15 @@ template<int mma_k_bytes=32, ducks::tt::all D, typename A, ducks::st_descriptor:
 __device__ static inline void mm2_ABt(D &d, const A &a, const B &b, const SA &sa, const SB &sb) {
     mma<transpose::N, transpose::T, D, A, B, SA, SB, 0, 2, mma_k_bytes>(d, a, b, sa, sb);
 }
+template<int mma_k_bytes=32, ducks::tt::all D, typename A, ducks::st_descriptor::input B, ducks::tt::all SA, ducks::tt::all SB>
+__device__ static inline void mm2_AB(D &d, const A &a, const B &b, const SA &sa, const SB &sb, semaphore &sem) {
+    mma<transpose::N, transpose::N, D, A, B, SA, SB, 0, 2, mma_k_bytes>(d, a, b, sa, sb, sem);
+}
+template<int mma_k_bytes=32, ducks::tt::all D, typename A, ducks::st_descriptor::input B, ducks::tt::all SA, ducks::tt::all SB>
+__device__ static inline void mma2_AB(D &d, const A &a, const B &b, const SA &sa, const SB &sb, semaphore &sem) {
+    mma<transpose::N, transpose::N, D, A, B, SA, SB, 1, 2, mma_k_bytes>(d, a, b, sa, sb, sem);
+}
+
 template<ducks::tt::all D, typename A, ducks::st_descriptor::input B>
 __device__ static inline void mm_AtB(D &d, const A &a, const B &b, semaphore &sem) {
     mma<transpose::T, transpose::N, D, A, B, 0>(d, a, b, sem);
